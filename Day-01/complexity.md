# Time and Space Complexity

## Time Complexity: O(n^2)

## Space Complexity: O(1)

where:
- n = number of elements

Explanation: Two nested loops are used to check every pair, so worst-case time is O(n²). Only a fixed-size result array (2 ints) is used, so space is O(1).