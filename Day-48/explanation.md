To solve Sum of Left Leaves, we use DFS (Depth First Search) with recursion.

A node is considered a left leaf when:

1.It is the left child of its parent.
2.It has no left child.
3.It has no right child.